import {connect} from "react-redux";
import Footer from "../../components/weather_widget/Footer";

const mapDispatchToProps = {
};

export default connect(state => state, mapDispatchToProps)(Footer);
